<% content_for :breadcrumb do %>
  <div class="col-12 no-padding">
    <nav aria-label="breadcrumb">
      <ol class="breadcrumb">
        <li class="breadcrumb-item" aria-current="page">
          <%= link_to root_path, target: "_top" do %>
            <span>Dashboard</span>
          <% end %>
        </li>
        <li class="breadcrumb-item active" aria-current="page">
          <span>Transactions</span>
        </li>
      </ol>
    </nav>
  </div>
<% end %>

<div class="card card__custom mb-4">
  <div class="card-header">
    <div class="row">
      <div class="col-6">
        <h4>Transactions</h4>
      </div>

      <div class="col-6 text-right">
        <% if current_user.admin? %>
          <%= link_to new_transaction_path, class: 'btn btn-info', id: "add-new-transaction", target: "_top" do %>
            <i class="fa fa-plus"></i> Add new transaction
          <% end %>
        <% end %>
      </div>
    </div>
  </div>

  <div class="card-body p-0">
    <table class="table" id="transactions-datatable" data-source="<%= transactions_path(format: :json) %>">
      <thead>
        <tr>
          <th scope="col">ISBN</th>
          <th scope="col">Book Title</th>
          <th scope="col">User</th>
          <th scope="col">Loan Date</th>
          <th scope="col">Period</th>
          <th scope="col">Status</th>
          <th scope="col" class="no-sort">Action</th>
        </tr>
      </thead>
      <tbody>
      </tbody>
    </table>
  </div>
</div>
